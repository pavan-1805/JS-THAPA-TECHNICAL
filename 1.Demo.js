console.log(
  "EXECUTION CONTEXT: we need a run time environment to execute a java script code OR the environment in which our javascript code is executed and is evaluated, that environment is known as EXECUTION CONTEXT"
);
console.log(
  "initially we have a default execution context i.e, global execution context"
);
console.log(
  "JACASCDRIPT engine creates the global execution context before it starts to execute any code"
);
console.log(
  "Variables and functions that is not inside any funtion. A new EXECUTION CONTEXT gets created every time a function executed \n global execution context is like other execution context but its created by default"
);
console.log(
  "it is associated with GLOBAL OBJECT, which means window object. \n ex: this === window; name == window.name"
);
console.log(" ");
console.log("EXECUTION STACK or CALLING STACK");
console.log('which is used to store all the execution context created during the code execution');